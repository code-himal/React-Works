import { Heart, ShoppingCart, Star } from "lucide-react";
export default function ProductCard({ item, onAddToCart, isSaved, onToggleWishlist, onQuickView }) { return <article className="product-card"><div className="product-image-wrap"><img src={item.image} alt={item.name} /><span>{item.badge}</span><button type="button" className={`save-button ${isSaved ? "saved" : ""}`} onClick={() => onToggleWishlist(item)} aria-label={`Save ${item.name}`}><Heart size={18} fill={isSaved ? "currentColor" : "none"} /></button><button type="button" className="quick-view" onClick={() => onQuickView(item)}>Quick view</button></div><div className="product-content"><p className="product-category">{item.category}</p><h3>{item.name}</h3><p className="product-description">{item.description}</p><div className="rating"><Star size={15} fill="currentColor" /> {item.rating} <small>(128 reviews)</small></div><div className="product-bottom"><strong>${item.price}</strong><button type="button" onClick={() => onAddToCart(item)} aria-label={`Add ${item.name} to cart`}><ShoppingCart size={18} /> Add</button></div></div></article>; }
