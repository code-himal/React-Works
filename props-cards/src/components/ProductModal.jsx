import { useEffect } from "react";
import { Check, ShoppingCart, Star, X } from "lucide-react";
export default function ProductModal({ item, onClose, onAddToCart }) {
  useEffect(() => { const closeOnEscape = (event) => { if (event.key === "Escape") onClose(); }; window.addEventListener("keydown", closeOnEscape); return () => window.removeEventListener("keydown", closeOnEscape); }, [onClose]);
  return <div className="modal-backdrop" role="presentation" onMouseDown={onClose}><section className="product-modal" role="dialog" aria-modal="true" aria-labelledby="product-modal-title" onMouseDown={(event) => event.stopPropagation()}><button type="button" className="modal-close" onClick={onClose} aria-label="Close product details"><X size={21} /></button><div className="modal-image"><img src={item.image} alt={item.name} /></div><div className="modal-content"><p className="eyebrow">{item.category}</p><h2 id="product-modal-title">{item.name}</h2><div className="rating"><Star size={16} fill="currentColor" /> {item.rating} <small>(128 verified reviews)</small></div><p>{item.description}</p><ul><li><Check size={17} /> Commercial-grade materials</li><li><Check size={17} /> 30-day easy returns</li><li><Check size={17} /> Fast, free shipping over $150</li></ul><div className="modal-bottom"><strong>${item.price}</strong><button type="button" onClick={() => { onAddToCart(item); onClose(); }}><ShoppingCart size={18} /> Add to cart</button></div></div></section></div>;
}
